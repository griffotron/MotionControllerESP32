#ifndef _CONSTANTS_h
#define _CONSTANTS_h

#include <stdint.h>

const uint64_t FRAMES_PER_SECOND = 20;

// Filesystem folder (no trailing slash) holding stored/cached sequence JSON files
constexpr const char* SEQUENCE_DIR = "/sequences";


#endif